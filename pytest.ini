[pytest]
markers =
    e2e: end-to-end quota, billing, and page consistency tests
    costly: invokes AI or consumes rewrite quota
    integration: requires live API with optional Supabase
    ai: tests that call the LLM
